closure:
do;
    test: procedure;
        declare x;
        test2: procedure(n);
            x = x + n;
            call writeln(x);
        end test2;

        call test2(1);
        call test2(2);
        call test2(3);
    end test;

    call test;
end closure;
